import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'np.com.needtechnosoft.dairymanagement',
  appName: 'DairyManagement',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
